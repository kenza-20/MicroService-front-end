export interface Employe {
  id: number;          // corresponds to the "id" field in the backend
  name: string;        // corresponds to the "name" field in the backend
  description: string; // corresponds to the "description" field in the backend
  role: string;        // corresponds to the "role" field in the backend

  grade: number;       // corresponds to the "grade" field in the backend
}
